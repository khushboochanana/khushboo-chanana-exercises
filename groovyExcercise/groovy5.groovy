String truth = "hiiii"
if(truth)
    println "test evaluted to true "
else println "test evaluted to false"

 truth = ""
 if(truth)
    println "test evaluted to true "
else println "test evaluted to false"

 truth = "null"
 if(truth)
    println "test evaluted to true "
else println "test evaluted to false"
List l=[]
 if(l)
    println "test evaluted to true "
else println "test evaluted to false"
 truth = 0
 if(truth)
    println "test evaluted to true "
else println "test evaluted to false"
int n=0
 if(n)
    println "test evaluted to true "
else println "test evaluted to false"